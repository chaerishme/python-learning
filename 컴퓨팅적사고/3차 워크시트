* 학번이 짝수로 끝나면 1,2번 / 홀수로 끝나면 1,3번을 작성하세요.
* 모든 프로그램에 주석(#)으로 설명을 추가하세요.

#1 시작값과 끝값을 입력받아 시작값과 끝값 사이의 3의 배수의 합을 구하는 프로그램을 작성하세요.

x = int(input("시작숫자를 입력해주세요: "))
y = int(input("끝숫자를 입력해주세요: "))

n = x                      # n을 시작숫자 x로 초기화
total = 0                  # total을 0으로 초기화

while n <= y :             # n이 끝값인 y보다 작거나 같도록 범위 설정
    if n % 3 == 0 :        # 3의 배수인 n값 찾기
        total += n         # 3의 배수인 n값을 total에 더하기
    n += 1                 # n을 1씩 증가

print("%d부터 %d까지의 3의 배수의 합은 %d입니다." %(x, y, total))



#3 입력받은 숫자의 약수를 구하세요.
n = int(input("숫자를 입력해주세요.:"))
x = 1                       # 나누는 수 x를 1부터 설정
count = 0                   # 약수의 개수를 넣을 count를 0으로 초기화

while x <= n :              # 나누는 수 x가 숫자 n보다 작거나 같도록 범위 설정
    if n % x == 0 :         # x가 n의 약수인 경우
        print(x)            # n의 약수 x 출력
        count += 1          # 약수 개수 1개 증가
    x += 1                  # 나누는 수 x를 1씩 증가시키기
    
print("%d의 약수의 개수는 %d개 입니다." %(n, count))
